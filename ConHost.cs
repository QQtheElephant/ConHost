namespace ConHost
{
    public class ConHost
    {
        // Varaibles
        public sys SystemIn;
        public sys SystemOut;

        // Main
        void _ConHost(sys In, sys Out, sys Host, host SystemHost)
        {
            SystemIn = In;
            SystemOut = Out;

        }
    }
}